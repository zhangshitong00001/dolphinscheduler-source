<%- aslJava %>
package org.apache.dolphinscheduler.plugin.task.<%= naming.lower %>;

import org.apache.dolphinscheduler.plugin.task.api.parameters.AbstractParameters;
import org.apache.dolphinscheduler.spi.params.base.ParamsOptions;
import org.apache.dolphinscheduler.spi.params.base.PluginParams;
import org.apache.dolphinscheduler.spi.params.base.Validate;
import org.apache.dolphinscheduler.spi.params.input.InputParam;

import java.util.ArrayList;
import java.util.List;

public class <%= naming.pascal %>Parameters extends AbstractParameters {

    /**
     * TODO: Add your task-specific parameter fields here
     * Example:
     * private String host;
     * private int port;
     */

    @Override
    public boolean checkParameters() {
        // TODO: Validate required parameters
        return true;
    }

    /**
     * Returns the plugin params for UI rendering.
     *
     * @return list of plugin params
     */
    public static List<PluginParams> getPluginParams() {
        List<PluginParams> params = new ArrayList<>();

        // TODO: Define your plugin params for the UI
        // Example:
        // params.add(InputParam.newBuilder("host", "host")
        //         .setPlaceholder("Enter host address")
        //         .addValidate(Validate.newBuilder().setRequired(true).build())
        //         .build());

        return params;
    }
}
